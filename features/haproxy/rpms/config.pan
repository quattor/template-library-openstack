unique template features/haproxy/rpms/config;

# Payload
"/software/packages/{haproxy}" = dict();

# Required for putting servers into maintenance or draining mode
"/software/packages/{socat}" = dict();
